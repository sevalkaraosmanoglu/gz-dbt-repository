version: 2
sources:
  - name: raw                # dbt alias (dataset için)
    database: majestic-cairn-482009-d7
    schema: gz_raw_data      # BigQuery dataset (gerçek isim)
    tables:
      - name: sales          # dbt alias
        identifier: raw_gz_sales   # BigQuery tablo adı

      - name: product
        identifier: raw_gz_product

      - name: ship
        identifier: raw_gz_ship